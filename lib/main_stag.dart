
import 'package:flutter/material.dart';
import 'package:livraison_express/config/app-config.dart';

void main() async{
  AppConfig stagConfig = AppConfig(appName: "Livraison", environment: "staging");

  // Widget app = await initializeApp(stagConfig);
}