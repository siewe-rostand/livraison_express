import 'package:flutter/material.dart';

class CustDialog extends StatelessWidget {
  const CustDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
